// Seed script — implemented in Phase 3 after full schema migration.
// See docs/02-DATABASE.md §6 for seed order and data.

async function main() {
  console.log('🌱 Seed not yet implemented — complete Phase 3 first.');
}

main()
  .catch(e => {
    console.error(e);
    process.exit(1);
  });
